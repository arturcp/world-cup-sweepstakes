<header>
	<h1>World Cup Women's Youse 2019</h1>
	<h2>Take your chances and guess all the scores</h2>
</header>

<div id="da-slider" class="da-slider">
	<div class="da-slide da-slide-current">
		<h2>Make your bet!</h2>
		<p>How good are you to predict the outcome of soccer games? Check your numerology guru and the horoscope for your star sign, you will need all the help you can get.</p>

    <% if user_signed_in? %>
      <% is_admin = current_user.tournament_admin?(@world_cup) %>
      <% link_text = 'My guesses' %>

      <% if is_admin %>
        <% link_text = 'Admin' %>
      <% end %>

      <%= link_to(link_text, games_tournament_path(@world_cup), class: 'da-link') %>
    <% else %>
      <%= link_to('Sign up', new_user_registration_path, class: 'da-link') %>

      <label class="da-login">
        <%= link_to('or login', new_user_session_path)  %>
      </label>
    <% end %>

		<div class="da-img hide-on-med-and-down">
      <img src="https://upload.wikimedia.org/wikipedia/pt/4/41/2019_FIFA_Women%27s_World_Cup.svg.png" alt="image01">
    </div>
	</div>
</div>
